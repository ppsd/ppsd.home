# ติดตามตำแหน่งโฟร์แมนต่อเนื่องทั้งวัน (เข้าระบบ ERP)

มี 2 ทาง — เลือกตามความพร้อม

---

## ✅ ทาง A) ใช้แอป GPS ฟรี (ทำได้เลย ไม่ต้อง build แอป) — แนะนำเริ่มที่นี่

ติดตามตำแหน่ง **ต่อเนื่องแม้ล็อกจอ** ส่งเข้า ERP เราโดยตรง โดยใช้แอปสำเร็จรูปฟรี

### ขั้นตอน (ทำครั้งเดียวต่อมือถือ)
1. แอดมิน/ผู้จัดการเข้า **บุคลากร/HR → แท็บ "ติดตามตำแหน่ง" → ปุ่ม "⚙️ ตั้งค่าแอปติดตามเบื้องหลัง"**
   → จะเห็น **ลิงก์เฉพาะของพนักงานแต่ละคน** กด**คัดลอก**
2. ที่มือถือโฟร์แมน (Android) ติดตั้งแอป **GPSLogger** (ฟรี — Play Store หรือ F-Droid)
3. ในแอป GPSLogger:
   - เข้า **Logging details → Log to custom URL** → เปิด
   - **URL**: วางลิงก์ของคนนั้นที่คัดลอกมา (เช่น `https://.../api/track/xxxxx?lat=%LAT&lon=%LON&acc=%ACC`)
   - **HTTP method**: GET
   - ตั้ง **Logging interval** เช่น ทุก 5 นาที (300 วินาที)
4. กลับหน้าหลัก GPSLogger → กด **Start logging**
5. ตั้งค่าให้แอปทำงานเบื้องหลังได้ (ปิด Battery optimization ให้ GPSLogger)

เสร็จแล้ว! ตำแหน่งจะส่งเข้า ERP เองทุก 5 นาทีแม้ล็อกจอ → ดูเส้นทางได้ที่ **HR → ติดตามตำแหน่ง**

> **iPhone**: ใช้แอป **Overland** หรือ **GPSLogger for iOS** ตั้งค่า custom URL แบบเดียวกัน
> **เช็คอิน/เช็คเอาท์**: ใช้หน้า `…/#checkin` (ที่ติดตั้งเป็นแอปไว้แล้ว) เหมือนเดิม

⚠️ **ลิงก์ติดตามเป็นความลับเฉพาะคน** อย่าเปิดเผย (ถ้าหลุด แอดมินกดสร้างใหม่ได้)

---

## 🛠️ ทาง B) แอปเดียวจบ (เช็คอิน/เอาท์ + ติดตาม) แบรนด์ PPSD — ต้อง build เป็นแอป Android

ถ้าอยากได้ **แอปเดียว** ที่พนักงานเปิดแล้วเช็คอิน/เอาท์ + ติดตามเบื้องหลังในตัว (ไม่ต้องลง GPSLogger แยก) ต้องสร้างเป็นแอป Android จริง:

- ใช้ **Capacitor** ห่อเว็บแอป PPSD + ปลั๊กอิน **background-geolocation**
- ส่งตำแหน่งเข้า endpoint เดิม `/api/kiosk/location` หรือ `/api/track/<token>` (ฝั่ง ERP พร้อมรับแล้ว)
- **สิ่งที่ต้องมี:** คนที่ทำเป็น (หรือจ้างฟรีแลนซ์ ~ไม่กี่ชั่วโมง) + เครื่องลง Android Studio + บัญชี Google Play ($25 ครั้งเดียว ถ้าจะขึ้นสโตร์ หรือแจกไฟล์ APK ลงเองก็ได้)
- **iOS** ต้องมี Mac + บัญชี Apple Developer ($99/ปี)

ฝั่ง ERP เราพร้อมรับข้อมูลแล้ว 100% — เหลือแค่ขั้นตอน build แอป ซึ่งผมเตรียมโครงโปรเจกต์ + โค้ดส่วนติดตามให้ได้ถ้าต้องการ (แจ้งได้)

---

## สรุป
- **อยากได้เลยวันนี้ ฟรี** → ทาง A (GPSLogger + ลิงก์เฉพาะคน)
- **อยากได้แอปเดียวแบรนด์ตัวเอง** → ทาง B (ลงทุนทำแอป Android)
