# PPSD ERP — ใช้นอกออฟฟิศ + ติดตั้งเป็นแอปในมือถือ

## ส่วนที่ 1) ติดตั้งเป็นแอปในมือถือ (PWA) — ทำได้เลย ฟรี

หลังเปิดโปรแกรม (`npm start`) แล้วเข้าหน้าเว็บได้ ให้ทำตามนี้ที่มือถือ:

**iPhone (Safari):**
1. เปิดเว็บแอปใน **Safari** (เช่น `http://192.168.x.x:3001` หรือลิงก์จาก Cloudflare)
2. กดปุ่ม **แชร์** (สี่เหลี่ยมมีลูกศรขึ้น) ด้านล่าง
3. เลือก **"เพิ่มไปยังหน้าจอโฮม" (Add to Home Screen)** → เพิ่ม
4. จะมีไอคอน **PPSD ERP** บนหน้าจอ กดเปิดได้เหมือนแอป (เต็มจอ ไม่มีแถบเบราว์เซอร์)

**Android (Chrome):**
1. เปิดเว็บแอปใน **Chrome**
2. กดเมนู **⋮** (มุมขวาบน)
3. เลือก **"ติดตั้งแอป"** หรือ **"เพิ่มลงในหน้าจอหลัก"** → ติดตั้ง
4. จะมีไอคอน **PPSD ERP** บนหน้าจอ

> ไม่ต้องผ่าน App Store / Play Store — เป็นแอปติดตั้งตรงจากเว็บ อัปเดตอัตโนมัติเมื่อแก้โปรแกรม

---

## ส่วนที่ 2) เข้าใช้จากนอกออฟฟิศ (ไม่ต้อง WiFi เดียวกัน)

### วิธีที่แนะนำ: Cloudflare Tunnel (ฟรี ได้ลิงก์ https)

**สิ่งที่ต้องมี:** คอมที่รันโปรแกรมต้องเปิดทิ้งไว้ + ต่อเน็ต

**ขั้นตอน:**
1. เปิดโปรแกรมก่อน — ดับเบิลคลิก `start.bat` (หรือ `npm start`) รอจนขึ้น "เปิดใช้งานแล้ว"
2. ดับเบิลคลิก **`tunnel.bat`**
   - ครั้งแรกมันจะดาวน์โหลด `cloudflared.exe` ให้อัตโนมัติ (รอสักครู่)
3. รอจนเห็นลิงก์แบบนี้ในหน้าต่างดำ:
   ```
   https://xxxx-xxxx-xxxx.trycloudflare.com
   ```
4. เอาลิงก์นั้นไปเปิดที่**มือถือ/คอมเครื่องไหนก็ได้ เน็ตอะไรก็ได้** (4G/5G/WiFi อื่น) → ใช้งานได้เลย
5. **ห้ามปิดหน้าต่าง tunnel.bat** — ปิดเมื่อไหร่ลิงก์จะใช้ไม่ได้

**ข้อควรรู้:**
- ลิงก์ `trycloudflare.com` จะ**เปลี่ยนทุกครั้งที่เปิด tunnel.bat ใหม่** (เหมาะกับทดลองใช้/ใช้ชั่วคราว)
- ถ้าต้องการ **ลิงก์ถาวร** (เช่น `erp.บริษัทคุณ.com`) ต้องสมัคร Cloudflare (ฟรี) + มีโดเมน แล้วตั้ง Named Tunnel — แจ้งได้ เดี๋ยวทำคู่มือให้
- โปรแกรมมีระบบล็อกอิน + กันเดา PIN อยู่แล้ว แต่ลิงก์นี้เป็นสาธารณะ ใครมีลิงก์ก็เปิดหน้า login ได้ (ต้องมี user/PIN ถึงเข้าได้) — ควรตั้ง PIN ให้ปลอดภัย

### ทางเลือกอื่น
- **Tailscale** — ปลอดภัยสูงสุด (เฉพาะเครื่องที่อนุญาต) แต่ต้องลงแอป Tailscale ทุกเครื่อง
- **เช่า Cloud** — ไม่ต้องเปิดคอมทิ้งไว้ แต่มีค่าใช้จ่ายรายเดือน + ต้องปรับระบบ

อยากใช้ทางไหนเพิ่มเติม บอกได้เลยครับ
