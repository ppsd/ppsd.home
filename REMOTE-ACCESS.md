# ใช้ PPSD ERP จากที่ไหนก็ได้ (นอกออฟฟิศ / มือถือ / หน้างาน)

ระบบยังรันอยู่บนเครื่องออฟฟิศเหมือนเดิม (ข้อมูลอยู่กับเรา) — แค่เปิด "ทางเข้า" จากข้างนอก
มี 2 วิธี เลือกอย่างใดอย่างหนึ่ง:

| | วิธีที่ 1: Tailscale (แนะนำ) | วิธีที่ 2: Cloudflare Tunnel |
|---|---|---|
| เหมาะกับ | ทีมงานบริษัท (พนักงานลงแอปได้) | อยากได้ลิงก์เว็บเปิดได้ทุกเครื่อง |
| ความปลอดภัย | สูงมาก — เข้าได้เฉพาะเครื่องที่อนุญาต | ดี (มี HTTPS) แต่ URL เป็นสาธารณะ |
| ค่าใช้จ่าย | ฟรี (ถึง 100 เครื่อง) | ฟรี + ค่าโดเมนปีละ ~350 บาท |
| ติดตั้งยากไหม | ง่ายมาก (~15 นาที) | ปานกลาง (~45 นาที) |
| ต้องมีโดเมนไหม | ไม่ต้อง | ต้อง (เช่น ppsd.co.th) |

---

## วิธีที่ 1: Tailscale (แนะนำ — ปลอดภัยสุด ง่ายสุด)

Tailscale สร้าง "เครือข่ายส่วนตัว" เชื่อมเครื่องออฟฟิศกับมือถือ/โน้ตบุ๊กของทีม
ผ่านอินเทอร์เน็ตแบบเข้ารหัส — คนนอกมองไม่เห็นระบบเลย เหมือนทุกคนนั่งอยู่ในออฟฟิศ

### ตั้งครั้งเดียวที่เครื่องเซิร์ฟเวอร์ (เครื่องที่รัน ERP)

1. สมัครบัญชีที่ https://tailscale.com (ใช้ Google/Microsoft ล็อกอินได้ · แผนฟรี)
2. ดาวน์โหลดและติดตั้ง Tailscale for Windows จาก https://tailscale.com/download
3. ล็อกอินด้วยบัญชีที่สมัคร → เครื่องจะได้ชื่อ เช่น `ppsd-server`
4. (แนะนำ) ในเว็บ Tailscale admin → Machines → เครื่องเซิร์ฟเวอร์ → เปิด **Disable key expiry**
   (ไม่งั้นทุก 6 เดือนต้องล็อกอินใหม่)

### ที่มือถือ/โน้ตบุ๊กของแต่ละคน

1. ลงแอป Tailscale (App Store / Play Store / Windows)
2. ล็อกอิน **บัญชีเดียวกับบริษัท** (หรือใช้ฟีเจอร์ invite ในหน้า admin เชิญอีเมลพนักงาน)
3. เปิดเบราว์เซอร์เข้า `http://ppsd-server:3001` (ใช้ชื่อเครื่องแทน IP ได้เลย)
   — ใช้ได้ทุกอย่างเหมือนนั่งในออฟฟิศ: ดูงาน คีย์ PR ตอกบัตร ดูสลิป

### ข้อดี/ข้อจำกัด
- ✅ คนที่ไม่ได้ลงแอป+ล็อกอิน เข้าไม่ได้เด็ดขาด (โจรเดารหัสจากอินเทอร์เน็ตไม่ได้เลย)
- ✅ ฟรี ไม่ต้องมีโดเมน ไม่ต้องตั้ง router
- ⚠️ ทุกเครื่องที่จะใช้ต้องลงแอป Tailscale ก่อน (ครั้งเดียวต่อเครื่อง)

---

## วิธีที่ 2: Cloudflare Tunnel (ลิงก์เว็บเปิดได้ทุกเครื่อง)

ได้ URL จริง เช่น `https://erp.ppsd.co.th` เปิดจากเครื่องไหนก็ได้โดยไม่ต้องลงแอป
เหมาะถ้าอยากส่งลิงก์ให้คนใช้ง่ายๆ — แลกกับการที่ URL เป็นสาธารณะ (มีระบบล็อกอิน+ล็อก PIN ผิดคุมอยู่ แต่แนะนำเปิด Cloudflare Access ทับอีกชั้น)

### ขั้นตอน

1. **มีโดเมน** (ซื้อที่ Cloudflare ได้เลย ~350 บาท/ปี) และย้าย DNS มาที่ Cloudflare (ฟรี)
2. ที่เครื่องเซิร์ฟเวอร์ ดาวน์โหลด `cloudflared.exe` จาก
   https://github.com/cloudflare/cloudflared/releases (เลือก windows-amd64)
   วางไว้ในโฟลเดอร์โปรแกรมนี้
3. เปิด Command Prompt ในโฟลเดอร์โปรแกรม แล้วรันทีละบรรทัด:
   ```
   cloudflared.exe tunnel login
   cloudflared.exe tunnel create ppsd-erp
   cloudflared.exe tunnel route dns ppsd-erp erp.ppsd.co.th
   ```
   (เปลี่ยน erp.ppsd.co.th เป็นโดเมนจริงของเรา)
4. สร้างไฟล์ `cloudflared-config.yml` ในโฟลเดอร์โปรแกรม:
   ```yaml
   tunnel: ppsd-erp
   credentials-file: C:\Users\<ชื่อผู้ใช้>\.cloudflared\<tunnel-id>.json
   ingress:
     - hostname: erp.ppsd.co.th
       service: http://localhost:3001
     - service: http_status:404
   ```
5. ทดสอบ: `cloudflared.exe tunnel --config cloudflared-config.yml run ppsd-erp`
   → เปิด https://erp.ppsd.co.th จากมือถือ (ปิด WiFi ใช้ 4G ทดสอบ) ต้องเข้าได้
6. ตั้งเป็น service ให้รันเองตอนบูต:
   ```
   cloudflared.exe service install
   ```
   (หรือเพิ่มใน Task Scheduler แบบเดียวกับตัว ERP — ดู SERVICE-SETUP.md)

### แนะนำเพิ่ม (สำคัญถ้าใช้วิธีนี้)
- เปิด **Cloudflare Access** (ฟรีถึง 50 คน): Zero Trust → Access → Applications →
  เพิ่ม erp.ppsd.co.th → กำหนดว่าต้องล็อกอินอีเมลบริษัท/รายชื่อที่อนุญาตก่อนถึงเว็บ
  = คนนอกเจอหน้า Cloudflare ล็อกอินก่อน ไม่ถึงระบบเราเลย
- เปลี่ยน PIN ทุกคนจากค่าง่ายๆ (1234) เป็นเลขที่เดายาก

---

## สิ่งที่ต้องรู้ทั้งสองวิธี

- **เครื่องเซิร์ฟเวอร์ต้องเปิดอยู่** ระบบถึงใช้ได้จากข้างนอก — ตั้งค่า Windows ไม่ให้ sleep
  (Settings → Power → Never sleep) และติดตั้ง ERP เป็น service แล้ว (install-service.bat)
- **ตอกบัตรด้วย GPS**: ถ้าเปิดกติกา geofence ไว้ พนักงานตอกจากนอกรัศมีออฟฟิศจะถูกบล็อกตามกติกาเดิม
  (ทำงานถูกต้องทั้งสองวิธี เพราะ GPS มาจากมือถือผู้ใช้ ไม่ใช่เครือข่าย)
- **สำรองข้อมูล**ยิ่งสำคัญขึ้นเมื่อใช้ทางไกล — เช็คแถบสุขภาพระบบหน้าแรกให้เขียวเสมอ
- อนาคตถ้าทีมโต/อยากเลิกพึ่งเครื่องออฟฟิศ ค่อยย้ายขึ้น VPS (~เดือนละ 200 บาท) —
  โปรแกรมนี้ย้ายได้เลย (ก๊อปโฟลเดอร์ + server/data ไปรันที่ไหนก็ได้ที่มี Node.js)
